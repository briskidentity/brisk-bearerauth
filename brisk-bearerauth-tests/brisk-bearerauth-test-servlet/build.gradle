plugins {
    id("bearerauth.java-conventions")
    id("application")
}

apply(from: "$rootDir/gradle/dependency-versions.gradle")

dependencies {
    implementation(project(":brisk-bearerauth-servlet"))
    implementation("org.eclipse.jetty:jetty-servlet:$testLibraryVersions.jetty")
}
