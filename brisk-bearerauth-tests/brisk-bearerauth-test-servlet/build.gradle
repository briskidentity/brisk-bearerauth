plugins {
	id("bearerauth.java-conventions")
	id("application")
}

dependencies {
	implementation(project(":brisk-bearerauth-servlet"))
	implementation("org.eclipse.jetty:jetty-servlet:9.4.+")
}

application {
	mainClass = "sample.ServletTestApplication"
}
